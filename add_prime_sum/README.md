課題名: add_prime_sum 必要なファイル: add_prime_sum.c 使用可能な関数: write, exit

正の整数を引数として受け取り、その数以下のすべての素数の合計を表示し、 改行を追加するプログラムを作成してください。

引数の数が 1 でない場合、または引数が正の数でない場合は、 0 を表示して改行を追加するだけにしてください。

はい、以下の例は正しいです。

例：

$>./add_prime_sum 5
10
$>./add_prime_sum 7 | cat -e
17$
$>./add_prime_sum | cat -e
0$
$>
